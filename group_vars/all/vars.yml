---
username: "<username>"

docker_dir: /mnt/storage/docker_apps
data_dir: /mnt/storage/data

puid: "<puid>"

pgid: "<pgid>"

ip_address: "<server_ip>"

timezone: "<timezone>"

wg_password: "<wg_pass>"


packages:
  - unzip
  - wget
  - curl
  - git
  - python3
  - python3-pip
  - smartmontools
  - python3-markdown 
  - jq
  - bc

pip_packages:
  - ansible
  - github3.py
  - docker

docker_dependencies:
  - ca-certificates
  - gnupg
  - curl

docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
